# Auto-generated by agentic-f3dasm
# Timestamp : 2026-06-08T14:52:46+00:00
# Model     : claude-haiku-4-5-20251001
# Run       : /Users/harrislab/Documents/GitHub/f3dasm/studies/agentic_black_box_8d/runs/20260608T141147
#
#!/usr/bin/env python3
"""
Replicate the best 8-dimensional black-box optimization result.
Loads the canonical experiment ledger and reports the best point found.
"""

from f3dasm import ExperimentData
import numpy as np

# Load the canonical experiment ledger
experiment_data_dir = r"/Users/harrislab/Documents/GitHub/f3dasm/studies/agentic_black_box_8d/runs/20260608T141147/experiment_data"
data = ExperimentData.from_file(project_dir=experiment_data_dir)

# Extract outputs (f values) and corresponding input parameters
f_values = data.output['f'].values
input_cols = ['x1', 'x2', 'x3', 'x4', 'x5', 'x6', 'x7', 'x8']
x_values = data.input[input_cols].values

# Find the minimum value and its index
best_idx = np.argmin(f_values)
best_f = f_values[best_idx]
best_x = x_values[best_idx]

# Report the result
print(f"Best point found: x = {best_x}")
print(f"Best objective value: f(x) = {best_f:.6f}")
print(f"Total evaluations in ledger: {len(f_values)}")

# Assertion: verify the headline number
assert abs(best_f - (-1.7381)) < 0.01, f"Expected best_f ≈ -1.7381, got {best_f}"
print("REPLICATED: Best minimum f(x) = {:.6f}".format(best_f))
